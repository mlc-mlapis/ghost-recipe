ghost:
  build:
    build:
      - echo "nothing needed here"
    deploy: [ ./ ]
  run:
    prepare:
      - npm install ghost-cli@latest -g
      - adduser ghost
      - usermod -aG sudo ghost
    start: su - ghost && ghost start
